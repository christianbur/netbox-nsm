# Permission anchors: TypeConfig (object config).
# Policy data lives in netbox-custom-objects (COT instances + nsm_config comments).
from .permission_anchors import *
from .object_link import LinkPropagationChoices
