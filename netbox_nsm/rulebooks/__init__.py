"""COT rulebook registry, views, rules grid, and virtual rulebook helpers.

Cell rendering:
  - ``cell_html`` — AG-grid / rules-table HTML (filter loupe, IPA magnifier, modes)
  - ``rules_pill_render`` — simple pill cells for layout previews

Tab/grid stack: ``rules_tab`` (package) → ``grid`` / ``rules_layout``.
"""
